"""
Gemma Swarm — System Prompts
==============================
Each agent's system prompt lives in its own .py file.
Each file exposes a single get_prompt() function.

To edit a prompt: open the relevant file and modify get_prompt().
The agent will pick it up on next run — no code changes needed.
"""
