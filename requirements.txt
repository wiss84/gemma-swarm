# Gemma Swarm — Requirements
# Python 3.11 recommended
# Install with: pip install -r requirements.txt

# ── Core LangChain / LangGraph ─────────────────────────────────────────────────
langgraph>=1.0.0
langchain-core>=1.2.0
langchain-google-genai>=4.0.0

# ── LangGraph Checkpointing ────────────────────────────────────────────────────
langgraph-checkpoint-sqlite>=2.0.0

# ── Slack ──────────────────────────────────────────────────────────────────────
slack-bolt>=1.21.0

# ── Web Search & Fetching ──────────────────────────────────────────────────────
ddgs>=9.11.2
httpx>=0.27.0

# ── Utilities ──────────────────────────────────────────────────────────────────
pydantic>=2.0.0
python-dotenv>=1.0.0
google-genai>=1.66.0
google-api-core
requests

# ── File processing ─────────────────────────────────────────────────────────
Pillow          # For converting images to PNG format
ffmpeg
pandas          # For CSV processing
openpyxl        # For .xlsx Excel file reading (required by pandas)
PyPDF2          # For PDF text extraction
python-docx     # For Word document (.docx) reading
beautifulsoup4  # For Web scraping
lxml            # For Excel spreadsheet processing
playwright      # For Web scraping (JS-heavy pages)
trafilatura     # For advanced web page content extraction (main content detection)
